services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: republica-verde-web:latest
    container_name: republica-verde-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
